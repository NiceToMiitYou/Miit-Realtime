'use strict';

module.exports = function ChatApp(miit) {

    this.onConnection = function(spark, user, team) {

    };

    this.onDisconnection = function(spark, user, team) {

    };

    this.identifier = function() {
        return 'APP_CHAT';
    };
};
